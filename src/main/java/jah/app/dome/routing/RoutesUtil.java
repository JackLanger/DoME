package jah.app.dome.routing;

/**
 * helper class to set routes to the views.
 */
public final class RoutesUtil {

  /**
   * Route to Index.html.
   */
  public static final String INDEX = "/index";

  /**
   * Prevents creation of {@link RoutesUtil} objects as class only contains static strings.
   */
  private RoutesUtil() {
    // helper method only containing static strings
  }

}
