package jah.app.dome.routing;

/**
 * helper class to set routes to the views.
 */
public final class RoutesUtils {

  /**
   * Route to Index.html.
   */
  public static final String INDEX = "/";
  public static final String TASKS = "/tasks";

  /**
   * Prevents creation of {@link RoutesUtils} objects as class only contains static strings.
   */
  private RoutesUtils() {
    // helper method only containing static strings
  }

}
